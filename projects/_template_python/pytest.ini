[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    smoke: quick launch / session checks
    ios: iOS-only cases
    android: Android-only cases
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
