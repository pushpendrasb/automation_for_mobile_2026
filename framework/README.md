# Shared Appium framework — add new app projects under ../projects/

See ../README.md for the full layout.

This package is consumed via:

```json
"dependencies": {
  "@mobile-automation/appium-core": "file:../../framework"
}
```

Do not put app-specific page objects or tests here.
